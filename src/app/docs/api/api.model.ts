export interface DevPortalAPI {
    id? : string,
    version? : number,
    title? : string,
    name : string,
    description : string,
    overview ? : any,
    gettingStarted ?: any,
    reference ? : any,
    swagger? : string
}